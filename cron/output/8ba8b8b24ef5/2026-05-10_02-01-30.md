# Cron Job: 每日自我整理维护

**Job ID:** 8ba8b8b24ef5
**Run Time:** 2026-05-10 02:01:30
**Schedule:** 0 2 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

自我整理和维护任务：

1. 清理缓存文件：
   - 删除 ~/.hermes/image_cache/ 中的旧图片（保留最近7天的）
   - 删除 ~/.hermes/cache/screenshots/ 中的旧截图（保留最近7天的）

2. 检查 ~/.hermes/skills/ 目录：
   - 查看所有技能文档是否有需要更新的地方
   - 检查是否有新学会的方法可以补充到技能文档

3. 读取 memory 内容：
   - 检查是否有过期、无效的记忆信息需要删除或更新

4. 日志记录：
   - 把本次整理的结果打印出来（删除了多少文件、整理了什么）

只需要做这些简单的维护工作，不需要联网搜索。如果发现问题记录下来即可。

## Response

**整理报告（2026-05-10）**

---

### 1. 缓存清理

| 目录 | 状态 |
|------|------|
| `~/.hermes/image_cache/` | 已有文件 0 个，无需清理 |
| `~/.hermes/cache/screenshots/` | 已有文件 2 个，均为 2 天前（保留中），无需清理 |

**结果：本次删除文件 0 个** — 缓存中无超过 7 天的旧文件。

---

### 2. Skills 目录

共 **369 个**文档文件，无新增需要更新的内容。

---

### 3. Memory 内容

`~/.hermes/memory/` 目录不存在，user / memory / skills 三个 memory 文件均为空（尚未写入过持久记忆）。

---

### 4. 备注

- 截图缓存中的 2 个文件（browser_screenshot_*.png，2026-05-07，约 2 天前）仍在 7 天保留期内，予以保留。
- 建议：如需使用 memory 功能，可通过 `memory` 工具初始化写入，否则不影响任何功能。
