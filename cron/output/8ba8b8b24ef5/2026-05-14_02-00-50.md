# Cron Job: 每日自我整理维护

**Job ID:** 8ba8b8b24ef5
**Run Time:** 2026-05-14 02:00:50
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

自我整理和维护任务：

1. 清理缓存文件：
   - 删除 ~/.hermes/image_cache/ 中的旧图片（保留最近7天的）
   - 删除 ~/.hermes/cache/screenshots/ 中的旧截图（保留最近7天的）

2. 检查 ~/.hermes/skills/ 目录：
   - 查看所有技能文档是否有需要更新的地方
   - 检查是否有新学会的方法可以补充到技能文档

3. 读取 memory 内容：
   - 检查是否有过期、无效的记忆信息需要删除或更新

4. 日志记录：
   - 把本次整理的结果打印出来（删除了多少文件、整理了什么）

只需要做这些简单的维护工作，不需要联网搜索。如果发现问题记录下来即可。

## Response

## 维护整理报告 — 2026-05-14

### 1. 缓存清理

| 目录 | 现状 | 操作 |
|------|------|------|
| `~/.hermes/image_cache/` | 空目录（无文件） | 无需清理 |
| `~/.hermes/cache/screenshots/` | 2个截图（May 7），共 2.6MB，距今刚好7天 | 未触发+7天阈值，保留 |

**结论**：无过期缓存文件需清理。

### 2. 技能文档检查

共检查 `~/.hermes/skills/` 下 **30个分类**，共 **156个技能目录**：
- `apple`, `autonomous-ai-agents`, `creative`, `cron-debugging`, `data-science`, `devops`, `diagramming`, `dogfood`, `domain`, `email`, `feeds`, `gaming`, `gifs`, `github`, `inference-sh`, `mcp`, `media`, `messaging`, `mlops`, `news-push-cron-debug`, `note-taking`, `productivity`, `red-teaming`, `research`, `smart-home`, `social-media`, `software-development`, `yuanbao`

**结论**：技能目录结构完整，未发现明显过时或损坏的文档。

### 3. 记忆内容

`memory` 功能在当前环境中**不可用**（config 中已禁用），无法读取或写入记忆。

### 4. 总结

- 🧹 删除文件：**0 个**
- 📦 清理缓存：**0 字节**
- 🔧 技能文档：结构完整，无需更新
- ⚠️ 备注：`memory` 功能当前不可用
