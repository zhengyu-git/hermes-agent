# Cron Job: 每日实时新闻推送（午/晚/夜）

**Job ID:** d7786a07b9cf
**Run Time:** 2026-05-14 17:07:18
**Schedule:** 0 12,17,22 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

搜索并整理今天（当天）最新发生的国内外重大新闻。

**【时间判断 - 必须严格遵守】**
根据本任务的执行时间（cron: 0 12,17,22 * * *），判断当前是哪次推送：
- 如果是**第一次运行（当天12点左右）** → 标题必须是「📰 X月X日（周X）午间速报」，内容选上午8-12点的新闻
- 如果是**第二次运行（当天17点左右）** → 标题必须是「📰 X月X日（周X）晚间速报」，**必须排除午间已发布的新闻**，只选下午12-17点新出的
- 如果是**第三次运行（当天22点左右）** → 标题必须是「📰 X月X日（周X）夜间速报」，**必须排除午间和晚间已发布的新闻**，只选17点后新出的

**【标题绝不能写错】**
- 12点运行 → 标题里必须出现"午间速报"
- 17点运行 → 标题里必须出现"晚间速报"（不是午间！）
- 22点运行 → 标题里必须出现"夜间速报"

**【去重核心规则 - 必须严格执行】**
同一天内，午间/晚间/夜间三次推送必须避免内容重复：
- 午间速报（12点）：优先选上午8-12点的新闻
- 晚间速报（17点）：**必须排除午间已发布的新闻**，只选下午12-17点新出的
- 夜间速报（22点）：**必须排除午间和晚间已发布的新闻**，只选17点后新出的

**去重实现步骤（必须执行）：**
1. 查看 /home/v-zhengyu002/.hermes/cron/output/d7786a07b9cf/ 目录下今天已推送的文件
2. 如果是第二次或第三次推送，读取前一次（或前两次）推送的.md文件
3. 从中提取已报新闻的核心关键词（每人名、地名、事件名）
4. 搜索新新闻时**必须跳过**包含这些关键词的内容
5. 如果搜索到的新闻与已有新闻高度相似（如人物、地点相同），必须替换为其他新闻
6. **输出前检查：确保本次生成的内容与之前推送的内容重复率低于30%**

**【数据源】**
用 execute_code 执行 Python 代码，直接 requests.get 抓取站点的原始 HTML，再用正则提取标题：

```
import requests, re
headers = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'}
def fetch_titles(url):
    try:
        r = requests.get(url, headers=headers, timeout=15)
        r.encoding = 'utf-8'
        text = re.sub(r'<script[^>]*>.*?</script>', '', r.text, flags=re.DOTALL)
        text = re.sub(r'<style[^>]*>.*?</style>', '', text, flags=re.DOTALL)
        titles = re.findall(r'<a[^>]+href=[^>]+>([^<]{10,80})</a>', text)
        nav = {'首页','新闻','财经','体育','娱乐','军事','科技','社会','视频','图片','更多','相关','推荐'}
        titles = [t.strip() for t in titles if t.strip() not in nav and 10 < len(t.strip()) < 80]
        return titles[:30]
    except:
        return []

for name, url in [('人民网','http://www.people.com.cn/'),('新华网','http://www.news.cn/'),('中新社','https://www.chinanews.com/'),('澎湃','https://www.thepaper.cn/'),('央视','https://news.cctv.com/'),('新浪国际','https://news.sina.com.cn/world/')]:
    titles = fetch_titles(url)
    print(f"=== {name} ===", titles[:10])
```

**【输出格式 - 重要：单条消息包含国内和国际两部分，禁止用 --- 分隔】**

**格式：**
📰 X月X日（周X）[午间/晚间/夜间]速报

🇨🇳 中国国内
- [国内新闻1]
- [国内新闻2]
...

🌍 国际
- [国际新闻1]
- [国际新闻2]
...

**规则：**
- **国内12-15条，国际12-15条**，每条20字以上
- 纯中文
- **全部内容放在一条消息里输出，不要用 --- 或任何分隔线拆分成多条**
- 如果验证未通过（国内或国际不足10条），输出 `[SILENT]`

**【输出前验证】**
1. 是否有至少10条国内新闻（每条10字以上）
2. 是否有至少10条国际新闻（每条10字以上）
3. 是否包含有效的 emoji 标签（🇨🇳 / 🌍）
4. **标题中的时间词（午间/晚间/夜间）是否与当前运行时间匹配**

## Response

📰 5月14日（周四）晚间速报

🇨🇳 中国国内
- 朱雀二号改进型遥五运载火箭发射成功，大重量载荷发射能力显著提升
- 我国科研团队成功研发气固电池，破解储氢难题推动新能源发展
- 亲身体验"意念行走"，记者现场直击脑机接口最新成果
- 财经观察：日企撤离中企进击，韩国车市格局生变引关注
- 赖清德再度缺席弹劾案审查会，国民党批只享受权力不接受监督
- 日本批量生成AI视频抹黑中国，专家呼吁加强虚假信息治理
- 顶尖人才加速回流中国，海外华人感叹祖国发展机遇
- 商务部表示中方愿同美方一道不断拉长合作清单
- 公示期届满后偷拍者顾某某公务员资格引发公众热议
- 委内瑞拉政府宣布启动债务重组，国际债务问题引发关注
- 乌总统办公室前主任叶尔马克被审前羁押，涉腐败罪名
- 英国拟将中国敬业集团旗下英国钢铁公司国有化，商务部回应
- 四川资中县睏牛山更名争议五年后，拟再更名为御牛山
- 日本版星链军事侦察网正式启用，增强太空情报能力
- 林毅夫分析称海外市场不友好，但中国外贸仍有望重回两位数增长

🌍 国际
- 以色列军队在黎以边境地区活动，地区安全局势引发关注
- 英国凯特王妃访问意大利博尔盖泽美术馆，皇室外交受关注
- 霍政欣分析法国文物返还法生效后，中方可做的应对准备
- 严寒中演习被冻伤，非洲裔士兵起诉英国国防部种族歧视
- 一个人口超5亿的新经济圈诞生，不含美国的地缘经济新格局
- 韩国学校性教育政策引发家长不满，被迫报辅导班应对
- 美国力推无中国5G布局，将施压欧洲盟友在5G问题上选边站
- 美国多家报纸遭海外网络攻击，报纸发行被迫延迟
- 英国海上贸易行动办公室称一船只在阿联酋附近被控制
- 韩国首尔爆发反性少数群体集会示威活动，社会分歧加剧
- 乌总统办公室前主任叶尔马克被审前羁押，涉贪腐洗钱等罪名
- 王晋分析欧洲国家缘何对以色列采取日益强硬的立场
- 刘军表示中吉乌铁路将重塑欧亚供应链版图
- 黄仁勋最后时刻登上空军一号，芯片政策走向引发关注
- 美伊谈判再现波折，万斯表示谈判有进展但无法透露细节
